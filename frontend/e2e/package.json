{
  "name": "frontend-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Bộ e2e Playwright cho frontend tĩnh. Frontend không còn build step nào — package.json này CHỈ phục vụ test, không phải dependency của site.",
  "scripts": {
    "test": "playwright test --config playwright.config.ts",
    "test:unit": "node unit/static-assets.test.mjs && node unit/forum-xss.test.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
